import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    position: 'relative',
    flex: 1,
  },
  captureContent: {
    position: 'relative',
    width: '100%',
    height: '100%',
    flex: 1,
  },
  rightButton: {
    position: 'absolute',
    right: 16,
    bottom: 24,
  },
})

export default styles
